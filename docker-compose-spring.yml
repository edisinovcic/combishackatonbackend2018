version: "3"
services:
    web:
        build:
          context: .
          dockerfile: Dockerfile
        ports:
          - "8080:8080"
        container_name: combis-hackaton-app
        networks:
          - default

