version: "3"
services:
    web:
        build:
          context: .
          dockerfile: Dockerfile
        ports:
          - "8080:80"
        container_name: combisHackatonApp
        networks:
          - default
